<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_search"
        android:title="Search"
        android:icon="@drawable/ic_search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always"
        />
    <item android:id="@+id/menu_addSchedule"
        android:title="Add a Schedule"
        app:showAsAction="collapseActionView"
        android:icon="@drawable/ic_playlist_add"
        />
    <item android:id="@+id/menu_mySubjects"
        android:title="My Subjects"
        app:showAsAction="collapseActionView"/>
    <item android:id="@+id/menu_addSubject"
        android:title="Add a subject"/>
</menu>